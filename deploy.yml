
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Chạy mỗi khi có code mới đẩy lên branch main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build
        env:
          API_KEY: ${{ secrets.API_KEY }} # Lấy API Key từ GitHub Secrets bạn đã thiết lập

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Thư mục chứa code sau khi build
          branch: gh-pages # Branch dùng để host website
